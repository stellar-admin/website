```razor
@{
  var months = Enumerable.Range(1, 12).Select(i => $"{i:00}").Select(i => new SelectListItem(i, i));
  var years = Enumerable.Range(2024, 2029).Select(i => new SelectListItem(i.ToString(), i.ToString()));
}

<sui-field-group>
  <sui-field-set>
    <sui-field-legend>Payment Method</sui-field-legend>
    <sui-field-description>
      All transactions are secure and encrypted
    </sui-field-description>
    <sui-field-group>
      <sui-input asp-for="Model.Name"/>
      <sui-input asp-for="Model.CardNumber"/>
      <div class="grid grid-cols-3 gap-4">
        <sui-select asp-for="Model.Month" asp-items="@months" />
        <sui-select asp-for="Model.Year" asp-items="@years" />
        <sui-input asp-for="Model.Cvv"/>
      </div>
    </sui-field-group>
  </sui-field-set>
  <sui-field-separator/>
  <sui-field-set>
    <sui-field-legend>Billing Address</sui-field-legend>
    <sui-field-description>
      The billing address associated with your payment method
    </sui-field-description>
    <sui-field-group>
      <sui-input asp-for="Model.IsBillingAddressSame" />
    </sui-field-group>
  </sui-field-set>
  <sui-field-set>
    <sui-field-group>
      <sui-textarea asp-for="Model.Comments"/>
    </sui-field-group>
  </sui-field-set>
  <sui-field orientation="Horizontal">
    <sui-button type="button">Submit</sui-button>
    <sui-button variant="Outline" type="button">
      Cancel
    </sui-button>
  </sui-field>
</sui-field-group>
```
