---
title: Field
description: Easily create form fields with labels, help text and input controls
---

import { Demo } from "@/components/demo";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

<Demo src="/demo/tag-helpers/field-intro.html">
  <include>./_include/field-intro.mdx</include>
</Demo>

## Usage

The `<sa-field>` family of Tag Helpers are designed for composing accessible forms. `<sa-field>` is the core wrapper for a single field.

<Demo src="/demo/tag-helpers/field-usage-field.html">
  <include>./_include/field-usage-field.mdx</include>
</Demo>

`<sa-field-content>` is a flex column that groups label and description. This component is useful when you use a field in horizontal orientation

<Demo src="/demo/tag-helpers/field-usage-field-content.html">
  <include>./_include/field-usage-field-content.mdx</include>
</Demo>

Wrap related fields with `<sa-field-group>`, and use `<sa-field-set>` with `<sa-field-legend>` for semantic grouping.

<Demo src="/demo/tag-helpers/field-usage-field-group.html">
  <include>./_include/field-usage-field-group.mdx</include>
</Demo>

## Explicit and implicit fields

The usage examples demonstrated so far require a lot of boilerplate when creating fields by specifying `<sa-field>`, `<sa-field-label>`, `<sa-field-description>`, etc. To improve the developer experience, all the StellarAdmin input controls such as `<sa-input>`, `<sa-select>`, `<sa-textarea>`, etc. can implicitly create fields using one of the following two techniques.

1. When using model binding (with the `asp-for` attribute), StellarAdmin will implicitly wrap the input inside a `<sa-field>` with the corresponding `<sa-field-label>` and `<sa-field-description>` inferred from data annotations. If your model contains errors related to the bound property, a `<sa-field-error>` component with the error message will also be added.

   ```razor
   <sa-input asp-for="Model.Name"/>
   ```

2. If you are not using model binding, you can also opt in to implicit fields by specifying any combination of the `label`, `description`, and `error` attributes. When doing this, your input will be wrapped inside a `<sa-field>` with the corresponding `<sa-field-label>`, `<sa-field-description>`, and `<sa-field-error>` automatically added.

   ```razor
   <sa-input label="First Name"
             description="First name as displayed on card"
             error="Enter the name exactly as it appears on the card"/>
   ```

In both cases mentioned above, you can opt out of implicit fields by setting the `render-field` attribute to `false`. In the example below, StellarAdmin will not create an implicit field, even though we're using model binding.

```razor
<sa-input render-field="false" asp-for="Model.Name"/>
```

Most of the examples in this document demonstrate both explicit and implicit mode. Notice how compact the implicit examples are compared to their explicit counterparts.

## Examples

### Implicit fields

Whenever you use any of the various field input Tag Helpers (such as `<sa-input>`, `<sa-textarea>`, `<sa-select>` etc.) along with data binding, StellarAdmin will implicitly create a field with a label, description, and placeholder for you. You can see this demonstrated in the example below which implements the same form as above, but using data binding.

<Demo src="/demo/tag-helpers/field-implicit.html">
<Tabs items={["Razor", "Model"]}>
    <Tab value="Razor">
      <include>./_include/field-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
      ```cs
      public class ImplicitModel
      {
          [Display(
              Name = "Card Number",
              Description = "Enter your 16-digit card number",
              Prompt = "1234 5678 9012 3456"
          )]
          public string? CardNumber { get; set; }

          [Display(Name = "Comments", Prompt = "Add any additional comments")]
          public string? Comments { get; set; }

          [Display(Name = "CVV")]
          public string? Cvv { get; set; }

          [Display(Name = "Same as shipping address")]
          public bool IsBillingAddressSame { get; set; }

public string? Month { get; set; }

          [Display(Name = "Name on Card", Prompt = "Ibn Battuta")]
          public string? Name { get; set; }

          public string? Year { get; set; }
      }
      ```
    </Tab>

  </Tabs>

</Demo>

### Input

Text and password inputs inside fields, each with a description below the control.

<Demo src="/demo/tag-helpers/field-input.html">
    <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
      <include>./_include/field-input.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
      <include>./_include/field-input-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class InputImplicitModel
        {
            [Display(
                Name = "Email",
                Description = "Your email address",
                Prompt = "e.g. ibn.battuta@rihlah.travel"
            )]
            [DataType(DataType.EmailAddress)]
            public string? Email { get; set; }

            [Display(
                Name = "Password",
                Description = "Must be at least 8 characters long.",
                Prompt = "••••••••"
            )]
            [DataType(DataType.Password)]
            public string Password { get; set; }

        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Textarea

A field with a textarea and a description.

<Demo src="/demo/tag-helpers/field-textarea.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-textarea.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-textarea-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class TextareaImplicitModel
        {
            [Display(
                Name = "Feedback",
                Description = "Share your thoughts about our service.",
                Prompt = "Your feedback helps us improve..."
            )]
            public string? Feedback { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Select

A select inside a field. The description sits below the control.

<Demo src="/demo/tag-helpers/field-select.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-select.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-select-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class SelectImplicitModel
        {
            [Display(Name = "Travel style", Description = "Select the type of trip you take most often.")]
            public string? TravelStyle { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Checkbox

Horizontal fields place the checkbox before its label. The field set groups related options, and a separator divides the sections.

<Demo src="/demo/tag-helpers/field-checkbox.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-checkbox.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-checkbox-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class CheckboxImplicitModel
        {
            [Display(Name = "Landmarks and attractions")]
            public bool ShowLandmarks { get; set; }

            [Display(Name = "Restaurants and bars")]
            public bool ShowRestaurants { get; set; }

            [Display(Name = "Local tour guides")]
            public bool ShowTourGuides { get; set; }

            [Display(Name = "Transit stations")]
            public bool ShowTransitStations { get; set; }

            [Display(
                Name = "Sync map with the itinerary",
                Description = "Places from the itinerary are shown on the map automatically. Changes you make here are visible to all travellers on the booking."
            )]
            public bool SyncMapWithItinerary { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Radio

Radios use horizontal fields inside a field set and share a `name` so only one can be selected.

<Demo src="/demo/tag-helpers/field-radio.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-radio.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-radio-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class RadioImplicitModel
        {
            public string? InsuranceCover { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Fieldset

Use `<sa-field-set>` with an `<sa-field-legend>` to group related fields under a heading, and lay them out with your own grid classes.

<Demo src="/demo/tag-helpers/field-fieldset.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-fieldset.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-fieldset-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class FieldsetImplicitModel
        {
            [Display(Name = "Street Address", Prompt = "12 Rue de la Kasbah")]
            public string? Street { get; set; }

            [Display(Name = "City", Prompt = "Marrakech")]
            public string? City { get; set; }

            [Display(Name = "Postal Code", Prompt = "40000")]
            public string? PostalCode { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

### Field group

Stack `<sa-field>` components with `<sa-field-group>`. Add `<sa-field-separator>` to divide them. When grouping checkboxes or radio buttons, you can add `data-slot="checkbox-group"` or `data-slot="radio-group"` to tighten up spacing between the individual inputs.

<Demo src="/demo/tag-helpers/field-field-group.html">
  <Tabs items={["Using Explicit Fields", "Using Implicit Fields", "Model"]}>
    <Tab value="Using Explicit Fields">
        <include>./_include/field-field-group.mdx</include>
    </Tab>
    <Tab value="Using Implicit Fields">
        <include>./_include/field-field-group-implicit.mdx</include>
    </Tab>
    <Tab value="Model">
        ```cs
        public class FieldGroupImplicitModel
        {
            [Display(Name = "Push notifications")]
            public bool EnableBookingPushNotifications { get; set; }

            [Display(Name = "Email notifications")]
            public bool EnableFlightEmailNotifications { get; set; }

            [Display(Name = "Push notifications")]
            public bool EnableFlightPushNotifications { get; set; }
        }
        ```
    </Tab>

  </Tabs>
</Demo>

## Accessibility

`<sa-field-label>` renders a real `<label>`. With `asp-for`, or with the `label` attribute on an input, the label's `for` attribute is wired to the input's `id` for you. When you write the label yourself without `asp-for`, set `for` to the input's `id`. `<sa-field-set>` and `<sa-field-legend>` render a native `<fieldset>` and `<legend>`, which is the right way to name a group of related inputs such as radio buttons.

`<sa-field-error>` renders with `role="alert"`. The description and error are not linked to the input automatically. If you want them announced together with the input, give them an `id` and reference it from `aria-describedby` on the input. Set `aria-invalid="true"` on an input that is not model bound to expose its error state.

## API Reference

### `<sa-field>`

Renders a `<div>` element that wraps a single form field.

<TypeTable
  type={{
    orientation: {
      description: "The layout direction of the field.",
      type: "FieldOrientation",
      typeDescription:
        "FieldOrientation.Vertical | FieldOrientation.Horizontal | FieldOrientation.Responsive",
      default: "FieldOrientation.Vertical",
    },
  }}
/>

### `<sa-field-label>`

Renders a `<label>` element.

<TypeTable
  type={{
    "asp-for": {
      description:
        "An expression to be evaluated against the current model. When the tag has no content, the label text is inferred from the bound property's [Display] metadata.",
      type: "ModelExpression",
    },
  }}
/>

### `<sa-field-description>`

Renders a `<p>` element with help text for a field, forwarding any [global
attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes).

<TypeTable
  type={{
    "asp-for": {
      description:
        "An expression to be evaluated against the current model. When the tag has no content, the description is inferred from the bound property's [Display] metadata. Nothing is rendered if there is none.",
      type: "ModelExpression",
    },
  }}
/>

### `<sa-field-error>`

Renders a `<div>` element with `role="alert"` that displays a validation
message, forwarding any [global
attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes).
It is hidden unless a validation error is present.

<TypeTable
  type={{
    "asp-for": {
      description:
        "An expression to be evaluated against the current model. When the tag has no content, the validation message for the bound property is rendered.",
      type: "ModelExpression",
    },
  }}
/>

### `<sa-field-title>`

Renders a `<div>` element for a non-`<label>` field title, forwarding any
[global
attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes).

### `<sa-field-content>`

Renders a `<div>` element that groups the label and description of a horizontal
field, forwarding any [global
attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes).

### `<sa-field-group>`

Renders a `<div>` element that stacks related fields, forwarding any [global
attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes).

### `<sa-field-separator>`

Renders a `<div>` element containing a horizontal separator, with any child
content rendered as text on top of the line.

### `<sa-field-set>`

Renders a `<fieldset>` element.

### `<sa-field-legend>`

Renders a `<legend>` element for a `<sa-field-set>`.

<TypeTable
  type={{
    variant: {
      description: "The visual style of the legend.",
      type: "FieldLegendVariant",
      typeDescription: "FieldLegendVariant.Legend | FieldLegendVariant.Label",
      default: "FieldLegendVariant.Legend",
    },
  }}
/>
