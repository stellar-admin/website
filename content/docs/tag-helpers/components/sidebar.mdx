---
title: Sidebar
description: Displays a sidebar to navigate inside the application
---

import { Demo } from "@/components/demo";

<Demo src="/demo/tag-helpers/sidebar-intro.html" mode="fullscreen">
  <include>./_include/sidebar-intro.mdx</include>
</Demo>

## Usage

Wrap the sidebar and your page content in a `<sa-sidebar-wrapper>`. The `<sa-sidebar>` holds the navigation — composed of `<sa-sidebar-header>`, `<sa-sidebar-content>` and `<sa-sidebar-footer>` — while `<sa-sidebar-inset>` holds the main page content. Navigation is built up from `<sa-sidebar-group>`, `<sa-sidebar-menu>`, `<sa-sidebar-menu-item>` and `<sa-sidebar-menu-link>`. Use `<sa-sidebar-trigger>` to toggle the sidebar.

```razor
<sa-sidebar-wrapper>
    <sa-sidebar>
        <sa-sidebar-header>
            ...
        </sa-sidebar-header>
        <sa-sidebar-content>
            <sa-sidebar-group>
                <sa-sidebar-group-label>Platform</sa-sidebar-group-label>
                <sa-sidebar-group-content>
                    <sa-sidebar-menu>
                        <sa-sidebar-menu-item>
                            <sa-sidebar-menu-link href="...">
                                <sa-icon name="layout-dashboard" />
                                <span>Dashboard</span>
                            </sa-sidebar-menu-link>
                        </sa-sidebar-menu-item>
                    </sa-sidebar-menu>
                </sa-sidebar-group-content>
            </sa-sidebar-group>
        </sa-sidebar-content>
        <sa-sidebar-footer>
            ...
        </sa-sidebar-footer>
    </sa-sidebar>
    <sa-sidebar-inset>
        <sa-sidebar-trigger />
        <!-- Main page content -->
    </sa-sidebar-inset>
</sa-sidebar-wrapper>
```

## Examples

### Inset variant

Set `variant="SidebarVariant.Inset"` to render the main content as a floating, rounded card inset from the edges of the screen.

<Demo src="/demo/tag-helpers/sidebar-inset-variant.html" mode="fullscreen">
  <include>./_include/sidebar-inset-variant.mdx</include>
</Demo>

### Floating variant

Set `variant="SidebarVariant.Floating"` to detach the sidebar from the edge of the screen and render it as a floating panel.

<Demo src="/demo/tag-helpers/sidebar-floating-variant.html" mode="fullscreen">
  <include>./_include/sidebar-floating-variant.mdx</include>
</Demo>

### Alternate trigger icon

Provide child content to `<sa-sidebar-trigger>` to override the default icon. Use the `in-aria-expanded` and `not-in-aria-expanded` variants to swap the icon based on whether the sidebar is open or closed.

<Demo src="/demo/tag-helpers/sidebar-alternate-trigger-icon.html" mode="fullscreen">
  <include>./_include/sidebar-alternate-trigger-icon.mdx</include>
</Demo>

### Right side

Set `side="SidebarSide.Right"` to anchor the sidebar to the right edge of the screen.

<Demo src="/demo/tag-helpers/sidebar-right-side.html" mode="fullscreen">
  <include>./_include/sidebar-right-side.mdx</include>
</Demo>

### Collapsible to icons

Set `collapsible="SidebarCollapsible.Icon"` to collapse the sidebar down to icons only, instead of hiding it entirely.

<Demo src="/demo/tag-helpers/sidebar-collapsible-icon.html" mode="fullscreen">
  <include>./_include/sidebar-collapsible-icon.mdx</include>
</Demo>

### Collapsible groups

Wrap a `<sa-sidebar-group>` in a `<sa-collapsible>` to let an entire section of the sidebar collapse and expand together.

<Demo src="/demo/tag-helpers/sidebar-collapsible-groups.html" mode="fullscreen">
  <include>./_include/sidebar-collapsible-groups.mdx</include>
</Demo>

### Collapsible items

Use `<sa-sidebar-menu-button>` together with `<sa-collapsible>` to create a menu item that expands to reveal nested sub-items.

<Demo src="/demo/tag-helpers/sidebar-collapsible-items.html" mode="fullscreen">
  <include>./_include/sidebar-collapsible-items.mdx</include>
</Demo>

### Sidebar in a dialog

Set `collapsible="SidebarCollapsible.None"` to render a sidebar inside a `<sa-dialog>`, for example as part of a settings screen.

<Demo src="/demo/tag-helpers/sidebar-sidebar-in-dialog.html" mode="fullscreen">
  <include>./_include/sidebar-sidebar-in-dialog.mdx</include>
</Demo>

## Accessibility

`<sa-sidebar-trigger>` renders a button labelled "Toggle Sidebar" whose `aria-expanded` state is kept in sync with the sidebar, and the mobile backdrop is a button labelled "Close sidebar". `<sa-sidebar-inset>` renders the page content in a `<main>` element.

The active menu link is only marked visually. Add `aria-current="page"` to the link for the current page so assistive technology announces it as well, and give a menu an `aria-label` when the page has more than one navigation region.

## API Reference

### `<sa-sidebar-wrapper>`

Renders a `<sel-sidebar>` web component that wraps the sidebar and page content and owns the open/collapsed state. Nested `<sa-sidebar-trigger>` elements toggle it through the Invoker Commands API. It honours a user-supplied `id` and generates one otherwise.

### `<sa-sidebar>`

Renders the sidebar panel itself as a `<div>` (including the mobile drawer and its backdrop).

<TypeTable
  type={{
    variant: {
      description: "The visual style of the sidebar.",
      type: "SidebarVariant",
      typeDescription:
        "SidebarVariant.Sidebar | SidebarVariant.Floating | SidebarVariant.Inset",
      default: "SidebarVariant.Sidebar",
    },
    side: {
      description: "The edge of the screen the sidebar is anchored to.",
      type: "SidebarSide",
      typeDescription: "SidebarSide.Left | SidebarSide.Right",
      default: "SidebarSide.Left",
    },
    collapsible: {
      description: "How the sidebar collapses.",
      type: "SidebarCollapsible",
      typeDescription:
        "SidebarCollapsible.Offcanvas | SidebarCollapsible.Icon | SidebarCollapsible.None",
      default: "SidebarCollapsible.Offcanvas",
    },
  }}
/>

### `<sa-sidebar-trigger>`

Renders a `<button>` that toggles the sidebar via the Invoker Commands API (`command="--toggle-sidebar"` targeting the wrapper). It shows a default panel icon unless you supply child content.

### `<sa-sidebar-header>`

Renders a `<div>` for content pinned to the top of the sidebar.

### `<sa-sidebar-content>`

Renders a `<div>` holding the scrollable body of the sidebar.

### `<sa-sidebar-footer>`

Renders a `<div>` for content pinned to the bottom of the sidebar.

### `<sa-sidebar-inset>`

Renders a `<main>` element holding the page content next to the sidebar.

### `<sa-sidebar-separator>`

Renders a horizontal `<div role="separator">` inside the sidebar.

### `<sa-sidebar-group>`

Renders a `<div>` for a section of the sidebar.

### `<sa-sidebar-group-label>`

Renders a `<div>` heading for a group.

### `<sa-sidebar-group-content>`

Renders a `<div>` holding a group's content.

### `<sa-sidebar-menu>`

Renders a `<ul>` containing menu items.

### `<sa-sidebar-menu-item>`

Renders an `<li>` for a single menu entry.

### `<sa-sidebar-menu-button>`

Renders a `<button>` styled as a menu entry — typically paired with `<sa-collapsible>` for expandable items.

<TypeTable
  type={{
    size: {
      description: "The size of the button.",
      type: "SidebarMenuButtonSize",
      typeDescription:
        "SidebarMenuButtonSize.Default | SidebarMenuButtonSize.Small | SidebarMenuButtonSize.Large",
      default: "SidebarMenuButtonSize.Default",
    },
    variant: {
      description: "The visual style of the button.",
      type: "SidebarMenuButtonVariant",
      typeDescription:
        "SidebarMenuButtonVariant.Default | SidebarMenuButtonVariant.Outline",
      default: "SidebarMenuButtonVariant.Default",
    },
    "is-active": {
      description: "Whether the button is rendered in the active state.",
      type: "bool",
      default: "false",
    },
  }}
/>

### `<sa-sidebar-menu-link>`

Renders an [`<a>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#attributes) element styled as a menu entry. It is marked active automatically when its routing attributes match the current route.

<TypeTable
  type={{
    size: {
      description: "The size of the link.",
      type: "SidebarMenuLinkSize",
      typeDescription:
        "SidebarMenuLinkSize.Default | SidebarMenuLinkSize.Small | SidebarMenuLinkSize.Large",
      default: "SidebarMenuLinkSize.Default",
    },
    variant: {
      description: "The visual style of the link.",
      type: "SidebarMenuLinkVariant",
      typeDescription:
        "SidebarMenuLinkVariant.Default | SidebarMenuLinkVariant.Outline",
      default: "SidebarMenuLinkVariant.Default",
    },
  }}
/>

#### Routing attributes

The menu link also accepts the same routing attributes as the ASP.NET Core [Anchor Tag Helper](https://learn.microsoft.com/en-us/aspnet/core/mvc/views/tag-helpers/built-in/anchor-tag-helper) for generating the `href`. Supply either an `href` or these attributes — not both.

<include>./_shared/routing-attributes.mdx</include>

### `<sa-sidebar-menu-badge>`

Renders a `<div>` badge (such as a count) alongside a menu entry.

### `<sa-sidebar-menu-sub>`

Renders a `<ul>` containing nested sub-items.

### `<sa-sidebar-menu-sub-item>`

Renders an `<li>` for a single sub-entry.

### `<sa-sidebar-menu-sub-button>`

Renders a `<button>` styled as a sub-entry.

<TypeTable
  type={{
    size: {
      description: "The size of the button.",
      type: "SidebarMenuSubLinkSize",
      typeDescription:
        "SidebarMenuSubLinkSize.Small | SidebarMenuSubLinkSize.Medium",
      default: "SidebarMenuSubLinkSize.Medium",
    },
    "is-active": {
      description: "Whether the button is rendered in the active state.",
      type: "bool",
      default: "false",
    },
  }}
/>

### `<sa-sidebar-menu-sub-link>`

Renders an [`<a>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a#attributes) element styled as a sub-entry, marked active automatically when its route matches. It accepts the same routing attributes as `<sa-sidebar-menu-link>` above.

<TypeTable
  type={{
    size: {
      description: "The size of the link.",
      type: "SidebarMenuSubLinkSize",
      typeDescription:
        "SidebarMenuSubLinkSize.Small | SidebarMenuSubLinkSize.Medium",
      default: "SidebarMenuSubLinkSize.Medium",
    },
  }}
/>
