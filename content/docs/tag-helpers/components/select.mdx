---
title: Select
description: Displays a list of options for the user to pick from
---

import { Demo } from "@/components/demo";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

<Demo src="/demo/tag-helpers/select-intro.html">
  <include>./_include/select-intro.mdx</include>
</Demo>

## Usage

Put native `<option>` elements inside `<sa-select>`. It renders a styled `<select>`.

```razor
<sa-select>
    <option value="...">...</option>
</sa-select>
```

## Examples

### Model binding

The `sa-select` Tag Helper has full support for ASP.NET Core model binding using the `asp-for` and `asp-items` attributes. When using model binding, StellarAdmin will automatically wrap the select inside a [Field](field) with the correct label, description, and placeholder text derived from data attributes. You can opt out of this behavior by setting the `render-field` attribute to `false`

<Demo src="/demo/tag-helpers/select-model-binding.html">
  <Tabs items={["Razor", "Model"]}>
    <Tab value="Razor">
      <include>./_include/select-model-binding.mdx</include>
    </Tab>
    <Tab value="Model">
    ```cs
    public class BookingFormModel
    {
        [Display(
            Name = "Cabin Class",
            Description = "Higher classes offer more legroom, priority boarding, and flexible refund policies."
        )]
        public CabinClass CabinClass { get; set; }
    }
    ```
    </Tab>
  </Tabs>
</Demo>

### Model binding validation

When using model binding, validation errors from [Model state](https://learn.microsoft.com/en-us/aspnet/core/mvc/models/validation) are displayed correctly.

<Demo src="/demo/tag-helpers/select-validation.html">
    <Tabs items={["Razor", "Model"]}>
        <Tab value="Razor">
        <include>./_include/select-validation.mdx</include>
        </Tab>
        <Tab value="Model">
        ```cs
        public class BookingFormValidationModel
        {
            [Display(
                Name = "Cabin Class",
                Description = "Higher classes offer more legroom, priority boarding, and flexible refund policies."
            )]
            [Required(ErrorMessage = "Please select a valid cabin class")]
            public CabinClass? CabinClass { get; set; }
        }
        ```
        </Tab>
    </Tabs>
</Demo>

### With field

If you are not using model binding, you can use the various [Field](field) Tag Helpers to wrap your select inside a field.

<Demo src="/demo/tag-helpers/select-field-explicit.html">
  <include>./_include/select-field-explicit.mdx</include>
</Demo>

### Implicit field

You can also implicitly wrap the select inside a [Field](field) using the shorthand syntax and setting the `label` and `description` attributes.

<Demo src="/demo/tag-helpers/select-field-implicit.html">
  <include>./_include/select-field-implicit.mdx</include>
</Demo>

### Manual validation

If you're not using ASP.NET Core model binding, you can manually indicate validation errors by setting the `aria-invalid` attribute to `true`. This will display a red border around the select.

The error message can be displayed using either a `sa-field-error` Tag Helper (when using explicit fields) or the `error` attribute (when using implicit fields).

<Demo src="/demo/tag-helpers/select-manual-validation-explicit.html">
  <Tabs items={["Using Explicit Field", "Using Implicit Field"]}>
    <Tab value="Using Explicit Field">
      <include>./_include/select-manual-validation-explicit.mdx</include>
    </Tab>
    <Tab value="Using Implicit Field">
      <include>./_include/select-manual-validation-implicit.mdx</include>
    </Tab>
  </Tabs>
</Demo>

### Sizes

Use the `size` attribute to control the size of the select.

<Demo src="/demo/tag-helpers/select-sizes.html">
  <include>./_include/select-sizes.mdx</include>
</Demo>

### Groups

Native `<optgroup>` elements are supported for grouping options.

<Demo src="/demo/tag-helpers/select-groups.html">
  <include>./_include/select-groups.mdx</include>
</Demo>

### Disabled

Add `disabled` to the select to prevent interaction.

<Demo src="/demo/tag-helpers/select-disabled.html">
  <include>./_include/select-disabled.mdx</include>
</Demo>

## Accessibility

`<sa-select>` renders a native `<select>`, so keyboard navigation and screen reader support are the browser's own. The chevron is decorative and hidden from assistive technology. With `asp-for` or the `label` attribute the label is wired to the select for you. Set `aria-invalid="true"` on a select that is not model bound to expose its error state.

## API Reference

### `<sa-select>`

The `<sa-select>` tag helper renders a native `<select>` element inside a
wrapper `<div>`. When `asp-for` or `asp-items` is set, it delegates to the
built-in [ASP.NET Core Select Tag
Helper](https://learn.microsoft.com/en-us/aspnet/core/mvc/views/working-with-forms#the-select-tag-helper)
to generate the options and selected value.

<TypeTable
  type={{
    "asp-for": {
      description: "An expression to be evaluated against the current model.",
      type: "ModelExpression",
    },
    "asp-items": {
      description:
        "A collection of SelectListItem objects used to populate the options.",
      type: "IEnumerable<SelectListItem>",
    },
    description: {
      description: "Description text for the implicit Field wrapper.",
      type: "string",
    },
    error: {
      description: "Error text for the implicit Field wrapper.",
      type: "string",
    },
    label: {
      description: "Label text for the implicit Field wrapper.",
      type: "string",
    },
    name: {
      description: "The name of the rendered <select> element.",
      type: "string",
    },
    "render-field": {
      description:
        "Whether to wrap the select in a Field. By default a field is rendered when asp-for, label, description, or error is set.",
      type: "bool",
    },
    size: {
      description: "The size of the select.",
      type: "SelectSize",
      typeDescription: "SelectSize.Default | SelectSize.Small",
      default: "SelectSize.Default",
    },
  }}
/>
