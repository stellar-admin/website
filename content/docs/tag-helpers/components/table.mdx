---
title: Table
description: Displays data in a tabular format
---

import { Demo } from "@/components/demo";

<Demo src="/demo/tag-helpers/table-intro.html">
  <include>./_include/table-intro.mdx</include>
</Demo>

## Usage

```razor
<sa-table>
    <sa-table-caption>...</sa-table-caption>
    <sa-table-header>
        <sa-table-row>
            <sa-table-head>...</sa-table-head>
        </sa-table-row>
    </sa-table-header>
    <sa-table-body>
        <sa-table-row>
            <sa-table-cell>...</sa-table-cell>
        </sa-table-row>
    </sa-table-body>
    <sa-table-footer>
        <sa-table-row>
            <sa-table-cell>...</sa-table-cell>
        </sa-table-row>
    </sa-table-footer>
</sa-table>
```

## Examples

### With border

Wrap the table in a bordered container to give it an outer border.

<Demo src="/demo/tag-helpers/table-border.html">
  <include>./_include/table-border.mdx</include>
</Demo>

### With Select

<Demo src="/demo/tag-helpers/table-select.html">
  <include>./_include/table-select.mdx</include>
</Demo>

### Row selection

Wrap the table in `<sa-table-selection>` to add row selection. A checkbox in the table's header row acts as the select-all, a checkbox in a body row selects that row, and selected rows are highlighted. The selection posts as ordinary checkbox form data, so no extra wiring is needed to submit it.

<Demo src="/demo/tag-helpers/table-row-selection.html">
  <include>./_include/table-row-selection.mdx</include>
</Demo>

## API Reference

### `<sa-table>`

The `<sa-table>` tag helper renders a [`<table>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/table)
wrapped in a scrollable `<div>` container. The `class` attribute is applied to
the `<table>` element; other attributes are applied to the container. It has
no attributes of its own.

### `<sa-table-caption>`

The `<sa-table-caption>` tag helper renders a [`<caption>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/caption).
It has no attributes of its own.

### `<sa-table-header>`

The `<sa-table-header>` tag helper renders a [`<thead>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/thead).
It has no attributes of its own.

### `<sa-table-body>`

The `<sa-table-body>` tag helper renders a [`<tbody>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/tbody).
It has no attributes of its own.

### `<sa-table-footer>`

The `<sa-table-footer>` tag helper renders a [`<tfoot>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/tfoot).
It has no attributes of its own.

### `<sa-table-row>`

The `<sa-table-row>` tag helper renders a [`<tr>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/tr).
It has no attributes of its own.

### `<sa-table-head>`

The `<sa-table-head>` tag helper renders a [`<th>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/th),
and forwards standard `<th>` attributes such as `colspan` and `scope`. It has
no attributes of its own.

### `<sa-table-cell>`

The `<sa-table-cell>` tag helper renders a [`<td>`
element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/td),
and forwards standard `<td>` attributes such as `colspan`. It has no
attributes of its own.

### `<sa-table-selection>`

The `<sa-table-selection>` tag helper adds row selection to the table it
wraps, rendered as a `sel-table-selection` web component. The checkbox in the
table's header row acts as the select-all; a checkbox in a body row selects
that row. Selected rows get `data-state="selected"`, the select-all reflects
the checked/indeterminate state, and the selection posts as ordinary checkbox
form data. From JavaScript, read the current selection from the element's
`selectedValues` property, or listen for its bubbling `selection-change`
event. It has no attributes of its own; it forwards any global HTML
attributes (such as `id`) to the rendered element.
