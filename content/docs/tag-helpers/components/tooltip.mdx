---
title: Tooltip
description: A popup that displays information related to an element when the element receives keyboard focus or the mouse hovers over it.
---

import { Demo } from "@/components/demo";

<Demo src="/demo/tag-helpers/tooltip-intro.html">
  <include>./_include/tooltip-intro.mdx</include>
</Demo>

## Usage

The `<sa-tooltip>` Tag Helper uses the [Interest Invokers](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API/Using_interest_invokers) and [Popover](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API/Using) APIs to display a tooltip for UI elements.

<Callout type="warn">
  The Interest Invokers API is still experimental. See the Limitations below.
</Callout>

```razor
<!-- Add the element to trigger the tooltip and specify the ID of
     the tooltip to trigger using the interestfor attribute -->
<sa-button interestfor="--example-tooltip">
    Tooltip trigger
</sa-button>
<!-- Add the tooltip and specify its id attribute -->
<sa-tooltip id="--example-tooltip">
    <!-- Add the text to display inside the tooltip -->
</sa-tooltip>
```

### Limitations

1. The Interest Invokers API is [not supported across all browsers](https://caniuse.com/wf-interest-invokers) yet. StellarAdmin includes the [mfreed7/interestfor](https://github.com/mfreed7/interestfor) polyfill to ensure it works across all browsers. Make sure you have included the StellarAdmin JavaScript file in your pages.
2. Not all elements can be used as an invoker element (the element that triggers the tooltip). Officially, the `<a>`, `<button>` and `<area>` elements are supported, but in our tests it also works with `<input>` elements. See [Supported Tag Helpers](#supported-tag-helpers) below for the Tag Helpers you can use.

We strongly recommend you [read about the Interest Invokers API on MDN](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API/Using_interest_invokers#creating_an_interest_invoker) to familiarise yourself with its use and limitations.

## Examples

### Position

Control the position of the tooltip relative to the trigger element using the `position` attribute. The default value is `PositionArea.Top`.

<Callout type="tip">
  You can visualize the various values by using the Chrome team's [Anchor
  Position Tool](https://chrome.dev/anchor-tool/).
</Callout>

<Demo src="/demo/tag-helpers/tooltip-position.html">
  <include>./_include/tooltip-position.mdx</include>
</Demo>

### Offset

Control the offset of the tooltip from the trigger element by adding a margin to the tooltip.

<Demo src="/demo/tag-helpers/tooltip-offset.html">
  <include>./_include/tooltip-offset.mdx</include>
</Demo>

### Delay

You can add a delay before opening and closing the tooltip by using the `interest-delay-start` and `interest-delay-end` CSS properties.

<Callout type="tip">
  If you're using the `mfreed7/interestfor` Polyfill, you will need to use the
  `--interest-delay-start` and `--interest-delay-end` CSS properties in addition
  to the above-mentioned properties.
</Callout>

<Demo src="/demo/tag-helpers/tooltip-delay.html">
  <include>./_include/tooltip-delay.mdx</include>
</Demo>

### Supported Tag Helpers

You can use `<sa-tooltip>` with the `<sa-button>`, `<sa-linkbutton>`, `<sa-input>`, `<sa-select>`, and `<sa-textarea>` Tag Helpers.

<Callout type="tip">
  The limitations regarding which HTML elements it can be used with is due to
  the [limitations](#limitations) of the Interest Invokers API.
</Callout>

<Demo src="/demo/tag-helpers/tooltip-elements.html">
  <include>./_include/tooltip-elements.mdx</include>
</Demo>

### JavaScript API

Since `<sa-tooltip>` uses the [Popover API](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API/Using), you can use the `showPopover()`, `hidePopover()` and `togglePopover()` methods to control visibility of the popover.

When using the JavaScript API, you also need to make sure you specify the `source` element to ensure the tooltip is aligned correctly with the UI element for which the tooltip is shown.

<Callout type="tip">
  The `togglePopover()` method does not work correctly on Chromium browsers when you use the `popover="hint"` attribute - which is the default value `<sa-tooltip>` uses. If you want to use `togglePopover()` on Chromium, you must override this attribute by setting it to `manual`, as you can see in the example below.
</Callout>

<Demo src="/demo/tag-helpers/tooltip-js-api.html">
  <include>./_include/tooltip-js-api.mdx</include>
</Demo>

### JavaScript events

The [Interest Invokers](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API/Using_interest_invokers) API fires the `interest` and `loseinterest` events on the `<sa-tooltip>` whenever it is shown or hidden respectively.

<Demo src="/demo/tag-helpers/tooltip-js-events.html">
  <include>./_include/tooltip-js-events.mdx</include>
</Demo>

## Accessibility

`<sa-tooltip>` renders a hint popover that the trigger shows through its `interestfor` attribute. The browser, or the bundled polyfill, shows it when the trigger is hovered or receives keyboard focus and hides it on Escape, so the tooltip is reachable without a mouse as long as the trigger itself is focusable.

Keep the tooltip text supplemental. It does not become the trigger's accessible name, so an icon-only trigger still needs its own `aria-label`.

## API Reference

### `<sa-tooltip>`

The `<sa-tooltip>` Tag Helper renders a `<div>` element with `popover="hint"`
(which you can override by supplying your own `popover` attribute), shown by an
invoker element whose `interestfor` attribute references the tooltip's `id`.

<TypeTable
  type={{
    position: {
      description:
        "The position of the tooltip relative to the trigger element.",
      type: "PositionArea",
      typeDescription:
        "PositionArea.TopCenter | PositionArea.TopSpanLeft | PositionArea.TopSpanRight | PositionArea.Top | PositionArea.LeftCenter | PositionArea.LeftSpanTop | PositionArea.LeftSpanBottom | PositionArea.Left | PositionArea.BottomCenter | PositionArea.BottomSpanLeft | PositionArea.BottomSpanRight | PositionArea.Bottom | PositionArea.RightCenter | PositionArea.RightSpanTop | PositionArea.RightSpanBottom | PositionArea.Right | PositionArea.TopLeft | PositionArea.TopRight | PositionArea.BottomLeft | PositionArea.BottomRight",
      default: "PositionArea.Top",
    },
  }}
/>
