```razor
@{
    var partClasses = new SwitchClassNames
    {
        Root = "travel-field",
        Label = "travel-label",
        Description = "travel-help",
        Control = "travel-switch-control",
    };
}

<sa-switch id="classes-alerts" label="Flight alerts" description="Receive updates when your itinerary changes." checked class-names="@partClasses" />
```
